export enum InputType {
    INPUT = 'input',
    TEXTAREA = 'textarea',
    SELECT = 'select',
    TEXT = 'text',
    PASSWORD = 'password',
    DATE = 'date',
    FILE = 'file',
}

export enum UserType {
    ANONYM = 'TRIAL',
    LOGGED = 'MAIN'
}